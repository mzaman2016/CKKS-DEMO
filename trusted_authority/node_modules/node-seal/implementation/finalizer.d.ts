interface DisposableResource {
    delete: () => void;
}
export declare function autoFinalize<T extends DisposableResource>(target: object, resource: T): {
    unregister: () => void;
    reregister: (newResource: T) => void;
};
export {};
